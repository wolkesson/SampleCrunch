using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Sample_Crunch.Controls.Tests
{
    [TestClass()]
    public class TimelineTests
    {
        [TestMethod()]
        public void AutoSelectInvervalTest()
        {
         //Assert.AreEqual< DateTimeIntervalType>(DateTimeIntervalType.Hours,   Timeline.AutoSelectInterval(200.0, TimeSpan.TicksPerHour * 11));
         //   Assert.AreEqual<DateTimeIntervalType>(DateTimeIntervalType.Minutes, Timeline.AutoSelectInterval(200.0, TimeSpan.TicksPerMinute * 11));
        }
    }
}